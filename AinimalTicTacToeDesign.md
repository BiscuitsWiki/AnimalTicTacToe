变种井字棋（属性宝可梦版）开发设计文档

一、游戏核心概述

本游戏为9格传统井字棋变种，核心融合宝可梦属性克制规则、自定义棋子创作、共用牌堆回合发牌机制，胜利判定在传统三连基础上增加待胜阻断环节，打造兼具策略性与创作性的对战玩法。

二、核心对战规则

1. 基础棋盘与落子：沿用3×3九格棋盘，对战双方分红、蓝两方，初始各持有3枚自行创作的精灵棋子。

2. 属性克制逻辑：完全参考宝可梦18属性克制体系（属性名称可直接使用宝可梦属性名，属性克制玩法本身不受版权保护，仅精灵形象受版权保护）。己方棋子属性克制对方棋子属性时，可将棋子叠放占领对方棋子所在格子；被克制则无法发起占领，普通属性关系仅正常落子。

3. 叠放占领规则：占领采用叠放制——占领方棋子叠放于目标格最上方，格子归属与显示均以最上层棋子为准；同一格最多叠放8个棋子，达到叠放上限的格子无法再被占领。

4. 落子限制：每次落子只能占据一个格子（落在空格，或通过克制叠放在对方棋子所在格），不能同时占领多个格子。

5. 手牌规则：双方首个行动回合各发起始手牌 3 张，此后每次轮到自己行动的回合开始时从牌堆自动抽 1 张加入手牌；手牌跨回合保留（落子消耗、剩余留到下个行动回合继续使用），玩家不可主动抽牌；牌堆抽完即止，不重洗。

6. 胜利判定规则：当某方任意3枚同方棋子连成一条直线时，先判定链路可否被阻断——若连线3格均已叠满8层（对手无法再叠放占领），立即判定发起方获胜；否则进入1回合待胜缓冲期，轮到对手行动回合时，对手需以克制属性棋子叠放在连线格尝试占领阻断，若对手整回合操作后仍无法打断三连状态，则判定发起连線方获胜。

7. 跳过、平局与认输判定：轮到行动的一方可选择跳过（skip）本回合的出牌——不落子、不消耗手牌，回合抽牌正常，攻守易边；待胜缓冲期内防守方跳过视为放弃阻断，三连方直接获胜。平局判定（满足其一即平）：① 棋盘九格全部叠满 8 层且无三连（board_full）；② 双方连续跳过（A 跳过后 B 紧跟着跳过，both_skip）——任意一方落子即重置连续跳过计数、对局继续（"棋盘下满即平"与"行动方无处可落自动判平（no_moves）"规则已移除，无处可落时由玩家自行跳过）。对局中任一方可主动认输（resign），确认后直接判负、对方获胜。

三、回合发牌共用牌堆机制

1. 共用牌堆生成：对战匹配成功后，从全服审核通过的公共棋子仓库中，随机抽取36张互不相同的棋子卡组成双方共用牌堆（36张卡各不相同，属性仍为宝可梦18属性体系，属性可重复；预设卡池为18属性 × 各2张共36张，保证克制关系完整覆盖）。

2. 回合抽牌规则：无手动抽牌操作。双方首个行动回合开始时各发起始手牌 3 张（先手开局即发，后手在首次轮到时发），此后每次轮到自己行动的回合开始时从共用牌堆顶自动抽 1 张加入手牌（手牌累加，不重发）；牌堆抽完即止、不重洗（整局最多消耗 36 张）。

2.1 最新一手标识：引擎在 MatchState.lastPlaced 中记录双方各自最近一次落子格（公开信息，随 game:state 全量下发），棋盘对双方最新一手格子做阵营色描边高亮并显示"红最近/蓝最近"角标，便于双方参考对方与自己的上一手。

四、自定义棋子创作与管理

1. 创作入口与名额：在主界面背包模块开放创作功能，单玩家初始拥有3次精灵创作名额，创作完成保存后可随时进入编辑界面二次修改。

2. 创作功能选项：点击背包内加号创作格子后，提供三种素材生成方式，分别为从相册上传图片、直接拍照生成、内置画板手动作画；同时支持修改精灵名称、从18种宝可梦属性中单选其一设定属性，编辑过程中可随意切换属性、更换素材，确认保存后锁定基础设定但仍支持后续二次编辑调整。

3. 素材审核机制：所有创作完成的棋子素材，提交后自动进入待审核队列，经运营审核符合公序良俗规范后，方可进入公共仓库或用于对战；未通过审核的素材退回创作者修改。

五、违规举报与处理系统

1. 对局举报功能：对战过程中，玩家发现对方使用的棋子素材违背公序良俗，可点击对应棋子发起举报，被举报棋子自动标记为特殊待审核状态。

2. 分级处理逻辑：区分普通待审核素材与被举报素材两条审核链路，被举报棋子经审核确认违规后立即手动下架；若单枚棋子累计被举报达到设定阈值，系统自动优先下架并触发审核流程，审核通过后方可恢复使用。

六、开发优先级建议

1. 第一阶段：实现3×3棋盘基础落子逻辑、宝可梦18属性克制占领判定、三连待胜阻断胜利规则。

2. 第二阶段：开发背包创作入口、相册上传/拍照/画板作画功能、素材初审流程。

3. 第三阶段：搭建共用30枚异属性牌堆抽卡机制、对局举报与违规下架处理系统。

七、项目进度（更新于 2026-08-30）

按里程碑记录实际完成情况，整体遵循"核心规则 → 联网对战 → 内容安全 → 正式登录 → 多端"的推进路线。

1. P1 核心规则与人机对战（已完成）

* 规则引擎：18 属性克制表、落子/叠放占领（上限 8 层）、三连待胜阻断（pendingWin 缓冲一回合）、回合抽牌（起始 3 张 + 每回合抽 1 张、手牌跨回合累加、牌堆耗尽不重洗）、跳过回合（待胜期守方跳过判负、双方连续跳过判平）、叠满 8 层/连续跳过判平、主动认输判负，引擎为纯 TS 实现（前后端共用同一份代码）。

* 单元测试覆盖克制、叠放、阻断、平局等核心分支。

* 对局页 UI：DOM + CSS 动画渲染，含手牌选择、落子高亮、战报日志。

* 人机对战：贪心策略 AI（可胜则胜/可阻断则阻断/优先中心），页面接入跑通。

* H5 构建验证通过。

1. P2 联网对战与创作工坊（已完成）

* P2.1 服务端骨架：NestJS + Prisma + SQLite；Piece REST API（提交/初审/公共池/我的棋子）与图片上传（本地 uploads 目录，生产可换 COS）。

* P2.1 创作工坊页：素材上传（相册）、名称/属性编辑、我的棋子列表；request 封装统一鉴权头。

* P2.1 对局牌堆接入公共池：匹配开局从审核通过棋子中抽 36 张组堆。

* P2.2 WebSocket 实时对战：原生 WS（@nestjs/platform-ws），服务端权威裁决（客户端只展示）；内存匹配队列、房间管理、视角化广播（对手手牌以 ? 占位）；battle 页支持 pvp 模式；双客户端端到端验证（撮合/回合同步/非法操作忽略/断线判负）。

* P2.3 持久化与重连：Match/MatchAction 落库（含完整棋谱）；战绩统计 API（胜/负/平）；断线宽限期重连（默认 60s，RECONNECT\_GRACE\_MS 可调，超时未归判负）；客户端自动重连（最多 8 次、3s 间隔）；主菜单战绩卡片。

1. P3 内容安全闭环（已完成）

* 举报下架：PieceReport 表（pieceId+reporterId 唯一约束防重复举报）、对局内长按棋子举报（6 个预设理由）、3 人举报阈值自动下架（approved → reported，退出公共池，进行中对局不受影响）、双链路人工裁决（回退上架清零计数 / 驳回）。

* 运营后台：零依赖单页管理台（<http://localhost:3000/admin/，admin> token 鉴权），统计看板（各状态棋子数/对局/举报）、待审核与被举报双队列可视化、通过上架与驳回（驳回必填原因）操作，浏览器实测全流程通过。

1. P3 正式登录（已完成）

* User 模型（provider: guest/wechat + openId 唯一）。

* 登录 API：POST /auth/guest（H5 设备号游客登录，同设备复用账号）、POST /auth/wechat（小程序 code2session，需 WX\_APPID/WX\_SECRET 环境变量）、GET /auth/me、PATCH /auth/profile（改昵称 1\~12 字）。

* 鉴权接入：REST 统一 Bearer token；WS queue:join / match:reconnect 支持 token 优先解析，对局中对手名显示登录昵称；棋子作者归属、举报者身份均取自登录态。

* 客户端 auth 服务：ensureLogin()（本地 token 校验 → 失效自动游客登录，服务不可达离线降级）、token 持久化、昵称展示。

* 端到端验证：登录/复用/改名/归属/WS 昵称同步/未配置微信明确报错，全部通过。

1. 小程序端（真机联调准备就绪，待真机验证）

* 网络地址配置化：weapp 产物自动指向电脑局域网 IP（config.ts 集中管理，IP 变化改一处），urlCheck 已关闭。

* build:weapp 产物构建验证通过；Taro 跨平台 API 复查无 H5 专属调用。

* 已知限制：游客模式（touristappid）不支持真机预览，需申请微信测试号（免费即时）替换 AppID；真机上如遇请求失败，需在手机上"打开调试"跳过域名校验。

1. 近期小迭代（2026-08-30）

* 预设卡池扩至 36 张（18 属性 × 各 2 张），DECK\_SIZE 同步 30 → 36，前后端 core 一致。

* 小迭代（2026-08-31）：胜利判定增强——三连链路 3 格均叠满时跳过待胜缓冲立即判胜（前后端引擎同步，客户端 27 例单测覆盖）；人机对战顶栏加"退出"按钮；匹配等待页加"取消匹配"按钮（queue:leave）；房间邀请按钮改自适应宽度修复文字换行。

* 断线重连补全（2026-08-31）：会话内闪断自动重连（退避重试）原有基础上，补齐"刷新/重进恢复"——开局前 match:reconnect 探测，命中未完成对局直接恢复棋面续玩；网关应答改带信封事件并附 inRoom 房间提示；room:join 支持同房间重进（房主/蓝方/观战者恢复原身份，不占新席位），对局中重进走对局/观战视角恢复 + 房间状态补发；60s 宽限期超时判负兜底不变。新增 smoke-reconnect.mjs 冒烟（18 项断言）。

* 数据落库补全（2026-08-31）：登录模式确定为零注册零登录（参考 HullQin 联机游戏方案，游客设备号即正式身份，取消邮箱/用户名注册计划）。新增 GET /game/history/:playerId?limit=20 历史对局 API（对手昵称/我方阵营/胜负/结束原因/时间，上限 50 条）与战绩页 pages/history（胜负平 + 胜率统计卡、对局列表，胜负按阵营配色）；主菜单加"我的战绩"入口、战绩卡可点击进入；玩法速览文案同步回合发牌制。

* 安全加固（2026-08-31）：① token 存储——库内只存 SHA-256 哈希，raw 仅登录响应下发一次，重登录轮换 token，旧明文行首次校验时懒迁移（老会话不掉线）；② 管理端鉴权统一恒时比较（timingSafeEqual），ADMIN\_TOKEN 未配置或 < 16 字符直接拒绝，鉴权失败改 401；③ 上传魔数校验（PNG/JPEG/WebP 文件头），伪造扩展名的非图片文件拒绝并删除落盘文件；④ 固定窗口限流（进程内）：登录 10 次/分/IP、上传与投稿 5 次/分/IP、举报 10 次/分/IP，超限 429；⑤ 安全响应头 nosniff/X-Frame-Options/Referrer-Policy。新增 11 个安全单测（限流器/恒时鉴权/token 哈希与懒迁移），运行时验证全部通过。多实例部署时限流器需换 Redis；图片对象存储与微信 imgSecCheck 初审仍为远期项。

* 创作工坊页补充顶栏返回按钮（H5 端无小程序原生导航栏，页面自带返回入口，H5/小程序通用）。

* H5 宽屏适配：内容区限宽居中模拟手机视窗，棋盘格子 aspect-ratio 保证正方形；状态栏上下布局（提示文字可换行、按钮在下）。

* 规则改版与对局体验（2026-09-09）：① 叠放上限 3 层 → 8 层（前后端引擎/AI 同步）；② 抽牌规则改为"起始 3 张 + 每回合抽 1 张、手牌跨回合累加、牌堆耗尽不重洗"（移除递增发牌与 36 张快照重洗）；③ 新增认输——对局中状态栏"认输"按钮 + 确认弹窗，确认后直接判负、对方获胜（game:resign 事件，落库 reason=resign，人机/联机通用）；④ 断线重连补全——刷新/重进主菜单时自动探测未完成对局（match:reconnect）直接续玩，人在未开局房间则回房间大厅；⑤ 对局提示增强——选中手牌后可落格绿色加粗虚线、不可落格灰色蒙层；己方三连待胜提示绿色加粗、敌方三连待打断提示红色加粗（闪烁）；⑥ 新增 no_moves 平局（行动方无处可落）。客户端 30 例 + 服务端 29 例单测、smoke-deal 21 项断言、认输/重连冒烟 9 项断言、直连后端验收 10/10 全部通过。

* 跳过与平局改版（2026-09-09 二次）：① 新增跳过操作——轮到行动时状态栏"跳过"按钮（game:skip 事件，人机本地结算/联机服务端裁决），不落子不消耗手牌、正常换边抽牌；待胜期守方跳过 = 放弃阻断，三连方直接获胜；② 平局逻辑重写——board_full 改为"九格全部叠满 8 层且无三连"（原"棋盘下满即平"移除），新增 both_skip"双方连续跳过"平局（A 跳过后 B 紧跟着跳过；任意一方落子即重置计数继续对局）；③ 移除 no_moves 自动平局——无处可落时由玩家自行跳过（人机模式 AI 无合法落子自动跳过）；④ MatchState 新增 lastSkipped 字段（落子重置），PlaceEvent 新增 skipped 事件（战报文案"X跳过了本回合"），棋谱记录 type=skip；⑤ 历史战绩平局原因文案更新（棋盘叠满/双方连续跳过），旧记录 no_moves 文案兼容。

1. 待办与下一步方向

* P4.1 + P4.2 坐席制房间与观战已完成（2026-08-30）：RoomService 坐席制模型（房主红方坐席/挑战者蓝方坐席/观战席上限 20、TTL 2h 回收、互斥校验）+ WS 事件 room:create/join/leave/start/host:transfer + 房间大厅（坐席卡/开始对局/转让房主/观战人数/复制邀请）+ 链接直达加入 + 观战只读视图（手牌脱敏、中途加入立即挂载）+ 终局回 waiting 可反复开局 + 房主主动/自动转让。单测 16 例、WS 冒烟与浏览器三端 e2e 验证通过。注意：WsAdapter 应答为裸 JSON 无事件信封，房间结果改用 room:created / room:joined 等带信封推送。待 P4.3 小程序分享卡片。

* 小程序真机验证（需用户提供测试号 AppID）。

* 部署准备（Docker Compose 已完成 2026-08-31；同源化改造完成 2026-08-31）：根目录 docker-compose.yml 一键编排——server 容器（NestJS + Prisma + SQLite，启动时 prisma db push 自动建表，数据卷 server\_data / server\_uploads 持久化）+ web 容器（Taro H5 构建产物由 nginx 托管）。同源化：客户端 H5 生产构建按同源相对地址访问（client/src/config.ts——API 为相对路径，WS 按页面协议自动选 ws/wss，dev/小程序仍直连 hostname:3000）；web 容器 nginx（client/nginx.conf）反代 /ws /uploads /auth /pieces /game /admin 到 server:3000（WS 透传 Upgrade 头、idle 超时 1h；/uploads 用 ^\~ 前缀避免被静态资源正则截走）。因此对外仅暴露 web 的 80 端口，server 的 3000 仅 compose 内网可达；套 HTTPS 只需外层证书反代或 Cloudflare Tunnel 指向 80，客户端自动切换 wss，无混合内容问题。配套文件：server/Dockerfile（node:22-alpine + pnpm，含 prisma generate/build）、client/Dockerfile（node:22-bookworm-slim 构建阶段 + nginx:alpine 运行两段式；Taro 4.2.1 的 @tarojs/binding 无 musl 变体，构建阶段不可用 alpine，2026-09-07 服务器部署实测踩坑）、client/nginx.conf、.env.docker.example（ADMIN\_TOKEN）。部署命令：cp .env.docker.example .env → docker compose up -d --build。compose 内预留 PostgreSQL 迁移注释段。本机未装 Docker，需在服务器上首次构建验证（nginx 反代规则建议用 docker compose config + 浏览器实测）。部署验收：根目录 acceptance.mjs（零依赖，Node ≥ 22 全局 fetch/WebSocket）——`node acceptance.mjs http://服务器IP` 自动验收 14 项（H5 首页/静态缓存头/运营后台/游客登录/登录态/工坊列表/战绩/历史/上传路由/uploads 静态/安全响应头透传/WS 快速匹配开局/WS 房间创建/3000 端口收敛），传 `http://localhost:3000` 则为直连后端模式跳过 Web 静态与端口检查；直连后端模式 10/10 实测通过（2026-08-31）。注意：验收的 WS 匹配对局会在断线超时判负后落库一条测试记录。CI（GitHub Actions，2026-08-31）：.github/workflows/ci.yml，push main / PR 触发，三个并行 job——server-test（服务端单测 + prisma generate/build + 纯环境变量启动 ci.db + acceptance.mjs 直连后端模式验收）、client-test（客户端 vitest）、client-build（Taro H5 生产构建，验证同源化配置可编译）；关键路径已本地模拟 CI 环境验证（无 .env 文件、纯环境变量 + ci.db + db push，验收 10/10 通过）。仓库尚未 git init，需推送到 GitHub 后 Actions 生效；根目录已补 .gitignore。完整服务器部署操作指南见根目录 DEPLOYMENT.md（选购/初始化/上传/部署/验收/备份恢复/HTTPS/排障全流程；compose 统一 `-p att` 项目名固定卷名）。

* 部署后续待办：域名 + HTTPS（外层反代或 Tunnel，客户端已就绪）、域名白名单（小程序 request/ws 合法域名）、PostgreSQL 切库（schema provider 迁移 + migrations）、数据落库扩展（房间/对局持久化）。

* 公网部署上线（2026-09-08）：腾讯云轻量（上海，Ubuntu 24.04，ubuntu 用户 SSH+密码），入口 http://182.254.221.57:8080/ 。完整部署模式验收 14/14 通过（H5/静态缓存/后台/登录/工坊/战绩/历史/上传/uploads/安全头/WS 匹配/WS 房间/3000 收敛）。部署实况：宿主机预装 Caddy 占 80 → .env 设 WEB_PORT=8080 绕开（compose ports 参数化）；代码以 git archive 打包 scp 上传（私有仓库免 token）。上线版本 f88fb23+（含 bookworm-slim 构建修复）。

* 正式发布前：token 明文存储换 JWT + 过期时间、图片上传换对象存储、微信 imgSecCheck 内容初审接入。

* 远期：Steam 版（Tauri 打包 Web 版 + Steamworks SDK 接入成就/好友邀请）。

八、房间对战与观战设计（P4，坐席制已实现）

> 目标：好友约战——房主创建房间生成房间码/分享链接，好友点击链接或输码加入房间。加入并不立即开局：双方先在坐席内就座（房主=红方坐席，首位挑战者=蓝方坐席），其余加入者进观战席；房主点击"开始对局"才正式开局，终局后房间回到等待态可反复开局。复用现有 WS 对战通道与服务端权威裁决。

1. 房间模型（服务端内存，开发期不落库）

* Room：roomId（6 位房间码，去除易混淆字符 0/O/1/I 的 32 字符集）、host（红方坐席=房主）、guest（蓝方坐席，可为空）、spectators（观战席，上限 20）、phase（waiting / playing）、matchId（进行中对局 id）、createdAt、TTL 定时器（2 小时无活动自动回收，可配置 ROOM\_TTL\_MS）。

* 生命周期：创建即 waiting（创建者入红方坐席）→ 挑战者加入入蓝方坐席 → 房主点"开始对局"进入 playing → 终局自动回 waiting（可反复开局）→ 房主退出且无坐席玩家时解散。

* 房主转让：主动转让（waiting 期房主发起，与蓝方坐席玩家座位互换；playing 期拒绝）与自动转让（房主退出/断线时转让给蓝方坐席玩家；无坐席玩家则解散房间并通知全员 room:closed）。

1. WS 协议扩展（GameGateway 新增事件，均带 token 鉴权）

* room:create → 返回 { roomId }；创建者即房主（红方坐席）。

* room:join { roomId } → 蓝方坐席空且未开局则入座（role=guest）；否则进观战席（role=spectator，上限 20）。对局进行中加入的观战者立即挂载到对局并收到观战视角快照。

* room:start { roomId } → 仅房主可发起；要求蓝方坐席已入座且未在对局中。开局后房主红方、坐席蓝方，观战席全员转播并挂载观战视角。

* room:host:transfer { roomId } → 仅房主可发起，仅 waiting 期有效；与蓝方坐席玩家座位互换。

* room:leave → 退出房间（waiting 期房主退出触发自动转让或解散；playing 期坐席玩家退出走既有断线宽限判负逻辑，观战者直接移除）。

* 广播：room:state（房间码/phase/双方坐席昵称/观战人数，全员推送）；对局事件复用现有 game:state 推送，观战者收到观战视角（双方手牌数量可见、手牌内容隐藏为 ?，与对战方视角化广播共用同一套脱敏逻辑）。

1. 分享链接与小程序卡片

* H5：shareUrl 形如 https\://<host>/#/pages/battle/index?mode=room\&role=join\&room=XXXXXX；大厅页提供"复制链接/复制房间码"（Taro.setClipboardData）。部署后可加二维码（qrcode 库离线生成，无外部依赖）。

* 小程序：battle 页配置 onShareAppMessage，路径携带 room 参数，好友点卡片直达房间（P4.3 待做）。

1. 客户端 UI（已实现）

* 房间大厅（battle 页 waiting 态）：房间码标题 + 红/蓝坐席卡（红方带"房主"标签，蓝方空位显示"等待入座…"）+ 观战人数 + 房主操作区（"开始对局"按钮在蓝方入座前置灰、"转让房主"按钮入座后出现）+ 复制邀请链接/房间码 + 退出房间。坐席玩家/观战者显示对应等待文案，房主转让后各端角色自动跟随 room:state 调整。

* 观战模式：battle 页只读态——隐藏落子交互与手牌卡片（显示双方手牌数量与"内容隐藏"），顶部显示"观战 · 红方 vs 蓝方"，状态栏显示"XX行动中（观战）"；对局结束显示胜方与"返回房间"回大厅。

* 终局处理：坐席玩家点"返回房间"回大厅（房间不解散），房主可再次开局；观战者同样返回大厅继续等待。

1. 安全与边界

* 观战连接发送 game:draw / game:place 等操作事件时服务端直接拒绝（playerOf 按 socket 匹配坐席玩家，观战 socket 不在 players 表内）。

* 断线重连：复用 match:reconnect（token/playerId 定位对局），成功后 RoomService.rebindSocket 重绑房间成员 socket，大厅广播可达。

* 房间码碰撞：创建时若码已存在则重新生成（重试 5 次后带时间戳兜底）。

* 与匹配队列互斥：同一玩家同时只允许在一个房间/队列中，后进入的请求拒绝并提示（in\_room / already\_in\_room）。

1. 实施拆分与进度

* P4.1 房间创建/加入/约战链接 ✅：RoomService（内存坐席制房间表）+ room:create/join/leave/start/host:transfer 事件 + 主菜单入口 + 房间大厅（坐席卡/开始/转让）+ H5 复制链接。单测 16 例覆盖坐席入座/观战加入/开始/转让/自动转让/终局回 waiting。

* P4.2 观战席 ✅：观战视角广播（手牌/牌堆脱敏）+ battle 页观战只读态 + 中途加入立即挂载 + 观战人数展示。WS 冒烟（建房→入座→观战→转让→开局→发牌/落子同步）与浏览器三端 e2e 验证通过。

* P4.3 小程序分享卡片与真机联调（待做）：onShareAppMessage 携带 room 参数、卡片直达房间、真机双人约战验证。

* P4.4（可选二期）：观战弹幕/表情、房间观战回放（复用 MatchAction 棋谱）。

* P5 规则改版：回合抽牌制 ✅（2026-09-09 二次改版）：移除手动抽牌/跳过抽牌（TURN\_DRAW 阶段与 game:draw / game:skip\_draw 事件删除），回合开始自动抽牌——双方首个行动回合各发起始 3 张，其后每回合抽 1 张，手牌跨回合累加、牌堆耗尽即止不重洗；dealt 事件（对手/观战视角牌面脱敏为"?"）与棋谱 deal 记录保留；MatchState.lastPlaced 记录双方最近落子格，棋盘阵营色描边 + "红最近/蓝最近"角标高亮；叠放上限 8 层；新增认输（resign）与行动方无处可落平局（no\_moves）终局类型。前后端引擎同步改造，客户端 30 例 + 服务端 29 例单测与 WS 冒烟（起始发牌/累加抽牌/手牌保留/lastPlaced/视角脱敏/认输/重连）全部通过。

